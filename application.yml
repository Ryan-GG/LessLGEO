spring:
  amqp:
    deserialization:
      trust:
        all: true
  rabbitmq:
    host: localhost
    port: 5672
    username: myuser
    password: secret

rabbit:
  topicName: "parser-to-reducer-exchange"
  queueName: "parser-to-reducer-queue"
  routingKey: "foo.bar.#"