CREATE TABLE if not exists employee (
code integer,
name varchar(250),
main varchar(250),
edge varchar(250),
alpha integer,
luminnance integer,
finish varchar(250),
created_at timestamp,
updated_at timestamp,
PRIMARY KEY (code)
);